import asyncio
import os
import logging
import uuid
from aiogram import Bot, Dispatcher, F, types
from aiogram.types import FSInputFile
from aiogram.filters import Command
from aiogram.exceptions import TelegramBadRequest

# Импортируем движок
from engine import DeobfuscatorEngine

# --- НАСТРОЙКИ ---
API_TOKEN = 'ТВОЙ_ТОКЕН_СЮДА'

logging.basicConfig(level=logging.INFO, format="%(asctime)s - %(name)s - %(message)s")
logger = logging.getLogger("DeobfBot")

bot = Bot(token=API_TOKEN)
dp = Dispatcher()
engine = DeobfuscatorEngine()

@dp.message(Command("start"))
async def cmd_start(message: types.Message):
    await message.answer(
        "🤖 **Roblox Deobfuscator V2**\n\n"
        "Скинь `.lua` или `.txt`.\n"
        "Я запущу его в песочнице и вытащу:\n"
        "🔹 Спрятанные строки (AssetID, HTTP)\n"
        "🔹 Глобальные переменные\n"
        "🔹 Исходный код (если возможно)\n\n"
        "🚀 Поддерживает MoonSec, IronBrew, Luraph.",
        parse_mode="Markdown"
    )

@dp.message(F.document)
async def handle_document(message: types.Message):
    user_id = message.from_user.id
    # Генерируем уникальное имя, чтобы файлы не путались
    unique_id = uuid.uuid4().hex[:8]
    file_name = message.document.file_name or "script.lua"
    
    input_path = f"temp_{user_id}_{unique_id}.lua"
    output_path = f"Deobf_{unique_id}_{file_name}.txt"

    status_msg = await message.answer("⏳ **Анализ начат...**", parse_mode="Markdown")
    
    # 1. ЗАХВАТ ЦИКЛА (Критическое исправление)
    # Мы берем ссылку на текущий asyncio loop, чтобы передать его в sync функцию
    loop = asyncio.get_running_loop()
    
    # Хак для передачи статуса из синхронного кода в асинхронный
    last_status = [""]
    
    def sync_status_updater(text):
        if text == last_status[0]: return
        last_status[0] = text
        
        async def edit_coro():
            try:
                if text:
                    await status_msg.edit_text(f"⚙️ **Статус:** {text}", parse_mode="Markdown")
            except: pass 
        
        # Безопасный вызов корутины из другого потока
        if not loop.is_closed():
            asyncio.run_coroutine_threadsafe(edit_coro(), loop)

    try:
        file = await bot.get_file(message.document.file_id)
        await bot.download_file(file.file_path, input_path)

        with open(input_path, "rb") as f: content = f.read()

        # 2. ЗАПУСК ДВИЖКА (В потоке)
        result_data = await asyncio.wait_for(
            asyncio.to_thread(engine.process, content, sync_status_updater),
            timeout=120.0 # Тайм-аут 2 минуты
        )

        with open(output_path, "wb") as f: f.write(result_data)

        out_file = FSInputFile(output_path)
        
        # Определяем итоговый вердикт для описания
        caption = "✅ **Анализ завершен!**"
        if b"DECOMPILED SOURCE" in result_data:
            caption += "\n📜 Код декомпилирован (успех)."
        elif b"DUMPED STRINGS" in result_data:
            caption += "\n🔎 Код скрыт, но строки и данные извлечены."
        
        await message.answer_document(out_file, caption=caption)

    except asyncio.TimeoutError:
        await status_msg.edit_text("❌ **Ошибка:** Скрипт завис или выполняется слишком долго.")
    except Exception as e:
        logger.error(f"Error: {e}")
        try: await status_msg.edit_text("❌ Ошибка при обработке файла.")
        except: pass
    finally:
        # Чистка мусора
        for p in [input_path, output_path]:
            if os.path.exists(p):
                try: os.remove(p)
                except: pass

async def main():
    logger.info("Бот запущен!")
    await bot.delete_webhook(drop_pending_updates=True)
    await dp.start_polling(bot)

if __name__ == "__main__":
    try:
        # Windows Loop Fix (если запускаешь на Windows)
        if os.name == 'nt':
            asyncio.set_event_loop_policy(asyncio.WindowsSelectorEventLoopPolicy())
        asyncio.run(main())
    except KeyboardInterrupt:
        print("Бот выключен.")
