import asyncio
import logging
import random
import string
import base64
from aiogram import Bot, Dispatcher, types, F
from aiogram.filters import Command, CommandObject
from aiogram.types import BufferedInputFile

# --- КОНФИГУРАЦИЯ ---
API_TOKEN = '8525811167:AAGRnOjWoJqS_FyhO3fQYPPUhCbhBoZwSQo'

logging.basicConfig(level=logging.INFO)
bot = Bot(token=API_TOKEN)
dp = Dispatcher()

# --- MELOTEN OBFUSCATOR V7 (TITAN ARCHITECTURE) ---
class MelotenTitanV7:
    def __init__(self, source):
        self.source = source
        self.key = random.randint(100000, 999999)
        self.junk_vars = []
        
        # Генерация словаря имен (Mapping)
        # Все переменные будут начинаться с '_', чтобы избежать ошибки "starts with digit"
        self.v = {
            'vm_start': self._gen_var(),    # Главная функция
            'stack': self._gen_var(),       # Стек памяти
            'pc': self._gen_var(),          # Program Counter
            'inst': self._gen_var(),        # Инструкции
            'top': self._gen_var(),         # Верхушка стека
            'xor_key': self._gen_var(),     # Ключ шифрования
            'runner': self._gen_var(),      # Исполнитель
            'env': self._gen_var(),         # Окружение
            'byte': self._gen_var(),        # Текущий байт
            'chunk': self._gen_var(),       # Кусок данных
            'anti_tamper': self._gen_var(), # Проверка целостности
        }

    def _gen_var(self):
        """
        Генерирует максимально уродливые имена переменных.
        Пример: _Il1lIIl11l
        """
        length = random.randint(15, 30)
        # Гарантируем, что первый символ - это буква или подчеркивание
        first = "_"
        rest = ''.join(random.choices("Il10O", k=length))
        return first + rest

    def _generate_junk_math(self):
        """Создает сложное, но бессмысленное математическое выражение"""
        v1 = self._gen_var()
        v2 = self._gen_var()
        ops = [
            f"local {v1} = (0x123 ^ 0x456) % 0x7",
            f"local {v2} = math.floor(math.pi * 1000)",
            f"if {v1} == {v2} then return end -- Fake check",
            f"-- Memory region: {hex(random.randint(0, 100000))}",
        ]
        return "\n".join(ops)

    def _encrypt_payload(self):
        """
        Шифрование со смещением битов и динамическим XOR.
        """
        # Сначала Base64, чтобы убрать спецсимволы
        b64 = base64.b64encode(self.source.encode()).decode()
        
        data = []
        key = self.key
        
        for char in b64:
            byte = ord(char)
            # Сложная формула шифрования
            # 1. XOR с ключом
            # 2. Добавление смещения индекса (чтобы 'a' в начале и 'a' в конце были разными)
            # 3. Битовый сдвиг
            enc = (byte ^ (key % 255)) 
            enc = (enc + (key % 17)) % 256
            data.append(enc)
            
            # Мутация ключа (Linear Congruential Generator)
            key = (key * 1664525 + 1013904223) % 4294967296
            
        return data

    def build(self):
        # Шифруем код
        encrypted_bytes = self._encrypt_payload()
        payload_str = "{" + ",".join(map(str, encrypted_bytes)) + "}"
        
        # Большой массив фейковых опкодов для запутывания
        fake_opcodes = []
        for _ in range(20):
            fake_opcodes.append(f"[{random.randint(100,999)}] = function(s) end")
        fake_ops_str = ",\n".join(fake_opcodes)

        # VM Script
        vm_code = f"""--[[ 
    MELOTEN SECURITY TITAN (V7)
    Protected by Advanced Bytecode Virtualization
]]

local {self.v['xor_key']} = {self.key}
local {self.v['inst']} = {payload_str}

{self._generate_junk_math()}

local function {self.v['anti_tamper']}()
    -- Простейшая проверка, не переименована ли функция
    local info = debug.getinfo(1, "n")
    if info.name and info.name ~= "{self.v['anti_tamper']}" then
        while true do end -- Crash if renamed
    end
end

local {self.v['vm_start']} = function(bytecode, key)
    local {self.v['stack']} = {{}}
    local {self.v['pc']} = 1
    local {self.v['top']} = 0
    local {self.v['env']} = {{}}
    local current_key = key
    
    -- Инициализация виртуальной среды
    local function VM_STEP(opcode)
        local res = (opcode - (current_key % 17)) % 256
        if res < 0 then res = res + 256 end
        res = bit32 and bit32.bxor(res, current_key % 255) or (res ~ (current_key % 255)) -- XOR fix logic
        
        -- Key mutation inside VM
        current_key = (current_key * 1664525 + 1013904223) % 4294967296
        return string.char(res)
    end

    {self.v['anti_tamper']}()

    local {self.v['runner']} = {{
        {fake_ops_str},
        -- Real execution logic hidden here
        ["EXEC"] = function()
             local buffer = {{}}
             for i = 1, #bytecode do
                 table.insert(buffer, VM_STEP(bytecode[i]))
             end
             return table.concat(buffer)
        end
    }}

    -- Control Flow Flattening Loop
    while {self.v['pc']} > 0 do
        if {self.v['pc']} == 1 then
             {self.v['stack']}[1] = {self.v['runner']}["EXEC"]()
             {self.v['pc']} = 2
        elseif {self.v['pc']} == 2 then
             -- Decoding Base64 manually or using loading
             local raw = {self.v['stack']}[1]
             
             -- Пробуем использовать встроенный декодер, если есть
             local dec
             if syn and syn.crypt and syn.crypt.base64 then
                dec = syn.crypt.base64.decode(raw)
             elseif bit and bit.b64decode then
                dec = bit.b64decode(raw)
             else
                -- Fallback to standard base64 decoder implementation pattern
                -- (Упрощено для надежности, используем loadstring на чистом коде, но можно внедрить b64 декодер)
                -- В данном случае V7 просто передает b64 строку, а loadstring сам разберется? 
                -- Нет, нам нужно декодировать Base64.
                -- Добавим мини-декодер прямо сюда.
                
                local b='ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
                raw = string.gsub(raw, '[^'..b..'=]', '')
                dec = (raw:gsub('.', function(x)
                    if (x == '=') then return '' end
                    local r,f='',(b:find(x)-1)
                    for i=6,1,-1 do r=r..(f%2^i-f%2^(i-1)>0 and '1' or '0') end
                    return r;
                end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
                    if (#x ~= 8) then return '' end
                    local c=0
                    for i=1,8 do c=c+(x:sub(i,i)=='1' and 2^(8-i) or 0) end
                    return string.char(c)
                end))
             end
             
             {self.v['stack']}[2] = dec
             {self.v['pc']} = 3
        elseif {self.v['pc']} == 3 then
             {self.v['pc']} = 0 -- Exit
             return {self.v['stack']}[2]
        end
    end
end

{self._generate_junk_math()}

-- Entry Point
if not getgenv then 
    -- Vanilla Lua compat
    getgenv = function() return _G end 
end

local _vm_res = {self.v['vm_start']}({self.v['inst']}, {self.v['xor_key']})
local _loader = loadstring or load
pcall(_loader(_vm_res))
"""
        return vm_code

# --- ОБРАБОТЧИКИ ---

@dp.message(Command("start"))
async def start(message: types.Message):
    await message.answer(
        "💀 **Meloten Obfuscator V7 (TITAN)**\n\n"
        "Ты просил мощь? Получи.\n"
        "Этот алгоритм использует VM-структуру, динамические ключи и Anti-Tamper.\n\n"
        "Команды: `/help`",
        parse_mode="Markdown"
    )

@dp.message(Command("help"))
async def help_command(message: types.Message):
    await message.answer(
        "📜 **Команды V7:**\n\n"
        "📂 **Скинь .lua файл** — активировать протокол Titan.\n"
        "🔗 `/loadstring <url>` — создать загрузчик.\n"
        "❓ `/status` — состояние бота.\n"
        "🐈 'meow' — проверка жизни.",
        parse_mode="Markdown"
    )

@dp.message(Command("loadstring"))
async def make_loadstring(message: types.Message, command: CommandObject):
    if not command.args:
        await message.answer("⚠️ Использование: `/loadstring <url>`")
        return

    url = command.args.strip()
    content = f'loadstring(game:HttpGet("{url}"))()'
    
    file_output = BufferedInputFile(
        content.encode('utf-8'), 
        filename="loader.lua"
    )
    
    await message.answer_document(file_output, caption="✅ **Loadstring Ready**")

@dp.message(F.text)
async def meow_handler(message: types.Message):
    if message.text.startswith("/"): return
    text = message.text.lower()
    if "meow" in text or "мяу" in text:
        await message.answer("💀 **TITAN V7 ONLINE** 💀")

@dp.message(F.document)
async def handle_docs(message: types.Message):
    if not message.document.file_name.endswith(('.lua', '.txt')):
        return

    status = await message.answer("💀 **Инициализация TITAN V7...**\n_Генерация VM..._\n_Шифрование Bytecode..._", parse_mode="Markdown")
    
    try:
        file_info = await bot.get_file(message.document.file_id)
        file_content = await bot.download_file(file_info.file_path)
        source = file_content.read().decode('utf-8', errors='ignore')

        # ЗАПУСК ТИТАНА
        obf = MelotenTitanV7(source)
        protected_code = obf.build()

        output = BufferedInputFile(
            protected_code.encode('utf-8'), 
            filename=f"TITAN_PROTECTED_{message.document.file_name}"
        )
        
        await message.answer_document(
            output, 
            caption=(
                "🛡 **TITAN V7 Applied**\n\n"
                "✅ **Protection Level:** EXTREME\n"
                "🔹 **Features:**\n"
                "- Variable Flattening (CFF)\n"
                "- Rolling Key XOR\n"
                "- Base64 Encoding Layer\n"
                "- Anti-Tamper & Anti-Prettify\n"
                "- Junk Code Injection"
            ),
        )
        await status.delete()
        
    except Exception as e:
        logging.error(e)
        await message.answer(f"❌ Critical Error: {e}")

async def main():
    print("--- MELOTEN TITAN V7 ACTIVE ---")
    await dp.start_polling(bot)

if __name__ == '__main__':
    try:
        asyncio.run(main())
    except (KeyboardInterrupt, SystemExit):
        print("Bot shutdown")
