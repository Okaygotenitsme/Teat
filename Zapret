import asyncio
import os
import logging
import sys

# Настройка логов, чтобы видеть всё в консоли
logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s - %(levelname)s - %(message)s",
    handlers=[logging.StreamHandler(sys.stdout)]
)
logger = logging.getLogger(__name__)

print("--- [DEBUG] Запуск скрипта... ---")

try:
    from aiogram import Bot, Dispatcher, F, types
    from aiogram.types import FSInputFile
    from engine import DeobfuscatorEngine
    print("--- [DEBUG] Импорты успешно загружены ---")
except ImportError as e:
    print(f"--- [ERROR] Ошибка импорта: {e}. Проверь: pip install aiogram lupa ---")
    sys.exit(1)

API_TOKEN = 'ТВОЙ_ТОКЕН' # УБЕДИСЬ, ЧТО ТУТ ТВОЙ ТОКЕН

if API_TOKEN == 'ТВОЙ_ТОКЕН':
    print("--- [WARNING] Ты забыл вставить API_TOKEN бота! ---")

bot = Bot(token=API_TOKEN)
dp = Dispatcher()
engine = DeobfuscatorEngine()

@dp.message(F.document)
async def handle_document(message: types.Message):
    logger.info(f"Получен файл от {message.from_user.id}")
    # ... (код обработки файла)
    await message.answer("Файл принят!")

async def main():
    print("--- [DEBUG] Попытка подключения к Telegram... ---")
    try:
        # Удаляем вебхуки, чтобы бот начал слушать сообщения
        await bot.delete_webhook(drop_pending_updates=True)
        print("--- [DEBUG] Бот вышел в онлайн и готов к работе! ---")
        await dp.start_polling(bot)
    except Exception as e:
        print(f"--- [ERROR] Ошибка при старте поллинга: {e} ---")

if __name__ == "__main__":
    try:
        asyncio.run(main())
    except KeyboardInterrupt:
        print("--- [DEBUG] Бот остановлен пользователем ---")
    except Exception as e:
        print(f"--- [CRITICAL] Ошибка запуска: {e} ---")
