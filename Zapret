import asyncio
import os
import logging
import uuid
from aiogram import Bot, Dispatcher, F, types
from aiogram.types import FSInputFile
from aiogram.filters import Command

# Импортируем движок
from engine import DeobfuscatorEngine

# --- НАСТРОЙКИ ---
API_TOKEN = 'ТВОЙ_ТОКЕН_СЮДА'

# Настройка логгера
logging.basicConfig(level=logging.INFO, format="%(asctime)s - %(name)s - %(message)s")
logger = logging.getLogger("DeobfBot")

# Инициализация бота и диспетчера
bot = Bot(token=API_TOKEN)
dp = Dispatcher()
engine = DeobfuscatorEngine()

@dp.message(Command("start"))
async def cmd_start(message: types.Message):
    await message.answer(
        "🤖 **Roblox Deobfuscator V3 (Mel Edition)**\n\n"
        "Скинь `.lua` или `.txt` файл.\n"
        "Я применю Deep Scan и Python String Extraction.\n\n"
        "🔹 Форматирование переменных: `local mel_1 = ...`\n"
        "🔹 Распознавание строк даже без декомпиляции\n"
        "🔹 Эмуляция Roblox API\n"
        "🔹 Защита от байт-код спама\n\n"
        "🚀 Отправляй файл!",
        parse_mode="Markdown"
    )

@dp.message(F.document)
async def handle_document(message: types.Message):
    user_id = message.from_user.id
    unique_id = uuid.uuid4().hex[:8]
    file_name = message.document.file_name or "script.lua"
    
    input_path = f"temp_{user_id}_{unique_id}.lua"
    output_path = f"Deobf_{unique_id}_{file_name}.txt"

    status_msg = await message.answer("⏳ **Загрузка и глубокий анализ...**", parse_mode="Markdown")
    
    loop = asyncio.get_running_loop()
    last_status = [""]
    
    def sync_status_updater(text):
        """Эта функция вызывается из engine.py"""
        if text == last_status[0]: return
        last_status[0] = text
        
        async def edit_coro():
            try:
                if text:
                    await status_msg.edit_text(f"⚙️ **Статус:** {text}", parse_mode="Markdown")
            except: pass 
        
        if not loop.is_closed():
            asyncio.run_coroutine_threadsafe(edit_coro(), loop)

    try:
        # 1. Скачиваем файл
        file = await bot.get_file(message.document.file_id)
        await bot.download_file(file.file_path, input_path)

        with open(input_path, "rb") as f: content = f.read()

        # 2. ЗАПУСК ДВИЖКА
        # run_in_executor для запуска тяжелой синхронной задачи
        result_data = await asyncio.wait_for(
            loop.run_in_executor(None, engine.process, content, sync_status_updater),
            timeout=180.0 # Увеличил тайм-аут до 3 минут, так как скан теперь глубже
        )

        # 3. Сохраняем результат
        with open(output_path, "wb") as f: f.write(result_data)
        out_file = FSInputFile(output_path)
        
        caption = "✅ **Анализ завершен!**"
        if b"local mel_1" in result_data:
             caption += "\n🔎 Найдены скрытые строки (Mel Format)."
        if b"DUMPED DATA" in result_data:
            caption += "\n📂 Данные извлечены."
        
        await message.answer_document(out_file, caption=caption)

    except asyncio.TimeoutError:
        await status_msg.edit_text("❌ **Ошибка:** Скрипт слишком сложный (тайм-аут 3 минуты).")
    except Exception as e:
        logger.error(f"Error processing file: {e}")
        try: await status_msg.edit_text("❌ Произошла ошибка. Попробуй другой файл.")
        except: pass
    finally:
        # Чистка
        for p in [input_path, output_path]:
            if os.path.exists(p):
                try: os.remove(p)
                except: pass

async def main():
    logger.info("Бот V3 Mel Edition запускается...")
    await bot.delete_webhook(drop_pending_updates=True)
    await dp.start_polling(bot)

if __name__ == "__main__":
    try:
        if os.name == 'nt':
            asyncio.set_event_loop_policy(asyncio.WindowsSelectorEventLoopPolicy())
        asyncio.run(main())
    except KeyboardInterrupt:
        print("Бот остановлен.")
