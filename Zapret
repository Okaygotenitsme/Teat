import telebot
import os
from obfuscator import obfuscate_script

API_TOKEN = 'ТВОЙ_ТОКЕН' # <--- НЕ ЗАБУДЬ ВСТАВИТЬ ТОКЕН
bot = telebot.TeleBot(API_TOKEN)

@bot.message_handler(content_types=['document'])
def handle_docs(message):
    file_name = message.document.file_name
    try:
        file_info = bot.get_file(message.document.file_id)
        downloaded = bot.download_file(file_info.file_path)
        
        # Вызываем нашу VM-генерацию
        result = obfuscate_script(downloaded)
        
        new_name = "VM_" + file_name
        with open(new_name, "w", encoding="utf-8") as f:
            f.write(result)
            
        with open(new_name, "rb") as f:
            bot.send_document(message.chat.id, f, caption="💀 **VM Obfuscation Complete**\nЭтот код работает внутри виртуального процессора.")
            
        os.remove(new_name)
    except Exception as e:
        bot.reply_to(message, f"Error: {e}")

bot.infinity_polling()
