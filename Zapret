import lupa
from lupa import LuaRuntime
import logging
import subprocess
import os
import tempfile
import re

# Логгер
logger = logging.getLogger("DeobfuscatorEngine")
logging.basicConfig(level=logging.INFO)

class DeobfuscatorEngine:
    def __init__(self):
        base_dir = os.path.dirname(os.path.abspath(__file__))
        self.unluac_path = os.path.join(base_dir, "unluac.jar")
        logger.info(f"📁 Working Dir: {base_dir}")

    def process(self, content, status_callback=None):
        def update_status(msg):
            if status_callback: status_callback(msg)
            logger.info(f"[STATUS] {msg}")

        if not self._check_java():
            return b"-- [FATAL] Java is missing."

        update_status("🧠 Запуск эвристического анализатора (MoonSec/IronBrew)...")

        # --- НАСТРОЙКА LUA VM ---
        try:
            lua = LuaRuntime(unpack_returned_tuples=True, encoding=None)
        except Exception as e:
            return f"-- [FATAL] Lua start error: {e}".encode('utf-8')

        # 1. Полифиллы (Bitwise operations)
        bit_polyfill = """
        local bit = {}
        function bit.tobit(x) x = x % 4294967296; if x >= 2147483648 then x = x - 4294967296 end; return x end
        function bit.bnot(x) return 4294967295 - x % 4294967296 end
        local function bit_oper(a, b, oper)
            local r, m, s = 0, 2^31, nil
            repeat s,a,b = a+b+m, a%m, b%m; r,m = r + m*oper%(s-a-b), m/2 until m < 1
            return r
        end
        function bit.band(a, b) return bit_oper(a, b, 4) end
        function bit.bor(a, b)  return bit_oper(a, b, 1) end
        function bit.bxor(a, b) return bit_oper(a, b, 3) end
        function bit.lshift(a, b) return (a * 2^b) % 4294967296 end
        function bit.rshift(a, b) return math.floor(a / 2^b) % 4294967296 end
        package.loaded.bit = bit; _G.bit = bit; _G.bit32 = bit 
        """

        # 2. Среда-шпион (Spyware Environment)
        # Мы подменяем функции, чтобы воровать строки и действия скрипта
        sandbox_setup = bit_polyfill + """
        _G.CAPTURED_CHUNKS = {}
        _G.DETECTED_STRINGS = {}
        _G.ACTION_LOG = {}
        
        local function log_action(msg) table.insert(_G.ACTION_LOG, msg) end
        local function capture_str(s) 
            if type(s) == "string" and #s > 4 then 
                -- Игнорируем системный мусор
                if not s:find("stack begin") and not s:find("upvalue") then
                    _G.DETECTED_STRINGS[s] = true 
                end
            end 
            return s
        end

        -- Хук на string.char (часто используется для расшифровки)
        local real_char = string.char
        string.char = function(...)
            local res = real_char(...)
            capture_str(res)
            return res
        end
        
        -- Хук на table.concat (сборка строк)
        local real_concat = table.concat
        table.concat = function(t, sep, i, j)
            local res = real_concat(t, sep, i, j)
            capture_str(res)
            return res
        end

        -- Хук на load (захват кода)
        local real_load = load
        local function hooked_load(chunk, name, mode, env)
            if type(chunk) == "string" and (#chunk > 100 or chunk:sub(1,4) == "\27Lua") then
                table.insert(_G.CAPTURED_CHUNKS, {data = chunk, size = #chunk})
            end
            return real_load(chunk, name, mode, env)
        end

        -- PROXY ОБЪЕКТЫ (Эмуляция Roblox)
        local function create_proxy(name)
            local proxy = {}
            local meta = {}
            meta.__index = function(t, k)
                local key = tostring(k)
                log_action("Read: " .. name .. "." .. key)
                
                if key == "PlaceId" then return 123456 end
                if key == "Name" then return "Player" end
                if key == "UserId" then return 1001 end
                
                -- Возвращаем прокси дальше, чтобы скрипт не падал
                return create_proxy(name .. "." .. key)
            end
            meta.__newindex = function(t, k, v)
                log_action("Write: " .. name .. "." .. tostring(k) .. " = " .. tostring(v))
            end
            meta.__call = function(t, ...) 
                log_action("Call: " .. name .. "(...)")
                return create_proxy(name) 
            end
            meta.__tostring = function() return name end
            setmetatable(proxy, meta)
            return proxy
        end

        local env = {
            loadstring = hooked_load, load = hooked_load,
            print = function(...) local args={...}; log_action("Print: "..tostring(args[1])); end,
            warn = function(...) local args={...}; log_action("Warn: "..tostring(args[1])); end,
            string = string, math = math, table = table, 
            bit = bit, bit32 = bit, pairs = pairs, ipairs = ipairs, next = next,
            type = type, tostring = tostring, tonumber = tonumber, pcall = pcall, xpcall = xpcall,
            select = select, error = error, unpack = unpack or table.unpack,
            setmetatable = setmetatable, getmetatable = getmetatable,
            
            -- Эмуляция Globals
            game = create_proxy("game"),
            workspace = create_proxy("workspace"),
            script = create_proxy("script"),
            getgenv = function() return {} end,
        }
        env._G = env
        _G.sandbox_env = env
        return env
        """

        try:
            update_status("🌪️ Исполнение скрипта (Tracing)...")
            env = lua.execute(sandbox_setup)
            
            # Запускаем скрипт с ограничением по времени (через process в main)
            # Тут используем pcall чтобы не крашнуть бота
            runner = lua.eval("""
                function(code, env)
                    local f, err = load(code, "chunk", "bt", env)
                    if not f then return false end
                    pcall(f) -- Игнорируем ошибки исполнения, нам нужны данные
                    return true
                end
            """)
            runner(content, env)

            # --- СБОР РЕЗУЛЬТАТОВ ---
            
            # 1. Достаем строки (самое ценное)
            strings_tbl = lua.globals().DETECTED_STRINGS
            detected_strings = list(strings_tbl)
            
            # 2. Достаем логи действий
            action_log = list(lua.globals().ACTION_LOG)
            
            # 3. Достаем чанки (байткод)
            chunks = lua.globals().CAPTURED_CHUNKS
            
            best_chunk = None
            max_size = 0
            if len(chunks) > 0:
                for i in range(1, len(chunks) + 1):
                    c = chunks[i]
                    if c.size > max_size:
                        max_size = c.size
                        best_chunk = c.data
            
            # Если не поймали новый чанк, работаем с исходным
            target_code = best_chunk if best_chunk else content
            
            return self._finalize_output(target_code, detected_strings, action_log, update_status)

        except Exception as e:
            return f"-- [CRASH ERROR]: {str(e)}".encode('utf-8')

    def _check_java(self):
        try:
            subprocess.run(["java", "-version"], stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
            return True
        except: return False

    def _finalize_output(self, code_bytes, strings, logs, callback):
        """Собирает финальный файл: Инфо + Код"""
        
        # 1. Декомпиляция
        if callback: callback("🧬 Декомпиляция ядра...")
        decompiled = self._try_decompile(code_bytes)
        
        final_text = ""
        
        # --- HEADER: EXTRACTED STRINGS ---
        final_text += "-- [[ DUMPED STRINGS (DECRYPTED) ]] --\n"
        final_text += "-- Values found in memory during execution:\n\n"
        for s in strings:
            # Чистим строки от мусора
            safe_s = str(s).replace('\n', ' ').replace('\r', '')
            final_text += f"-- {safe_s}\n"
            
        final_text += "\n" + "="*50 + "\n\n"
        
        # --- BODY: SOURCE CODE ---
        if decompiled:
            if callback: callback("💅 Beautifying code...")
            try:
                src = decompiled.decode('utf-8', 'ignore')
            except:
                src = decompiled.decode('latin-1', 'ignore')
            
            # Применяем улучшенный форматтер
            src = self._beautify_lua(src)
            final_text += src
        else:
            final_text += "-- [WARNING] Unluac failed. Dumped raw bytecode below.\n"
            final_text += str(code_bytes)

        # --- FOOTER: ACTION LOG ---
        final_text += "\n\n" + "="*50 + "\n"
        final_text += "-- [[ EXECUTION ACTION LOG ]] --\n"
        final_text += "-- What the script tried to do:\n\n"
        for l in logs[-100:]: # Последние 100 действий
            final_text += f"-- {l}\n"

        return final_text.encode('utf-8')

    def _try_decompile(self, bytecode):
        if not os.path.exists(self.unluac_path): return None
        try:
            with tempfile.NamedTemporaryFile(delete=False, suffix=".lua", mode='wb') as tmp:
                tmp.write(bytecode)
                path = tmp.name
            
            # Увеличиваем память Java для больших файлов
            res = subprocess.run(["java", "-Xmx1G", "-jar", self.unluac_path, path], capture_output=True)
            os.remove(path)
            
            if res.returncode == 0: return res.stdout
            return None
        except: return None

    def _beautify_lua(self, source_code):
        """
        Продвинутый Beautifier. 
        Убирает лишние переводы строк и делает отступы.
        """
        # Упрощаем структуру unluac
        source_code = re.sub(r'(\s)(local|return|if|for|while|repeat|function)(\s)', r'\n\2 ', source_code)
        
        # Замена для читаемости
        replacements = {
            " then ": " then\n",
            " do ": " do\n",
            " end ": "\nend\n",
            " repeat ": "\nrepeat\n",
            " until ": "\nuntil ",
            ";": ";\n"
        }
        for k, v in replacements.items():
            source_code = source_code.replace(k, v)

        lines = source_code.splitlines()
        formatted = []
        indent = 0
        
        for line in lines:
            line = line.strip()
            if not line: continue
            
            # Пропускаем строки с отладочной инфой unluac (если есть)
            if line.startswith("--"): 
                formatted.append(line)
                continue

            # Управление отступами
            if line.startswith(("end", "until", "}", "]", ")")):
                indent = max(0, indent - 1)
            elif line.startswith(("else", "elseif")):
                indent = max(0, indent - 1)
            
            formatted.append("\t" * indent + line)
            
            if line.endswith(("then", "do", "repeat", "{", "(")) or \
               ("function" in line and not line.endswith("end")):
                indent += 1
            
            if line.startswith(("else", "elseif")):
                indent += 1
                
        return "\n".join(formatted)
