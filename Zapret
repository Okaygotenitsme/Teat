local CheckFunction = Instance.new("RemoteFunction")
CheckFunction.Name = "CheckTeacherStatus"
CheckFunction.Parent = ReplicatedStorage

CheckFunction.OnServerInvoke = function(player)
	local canBe, reason, timeLeft = _G.CanBeTeacher(player)
	return canBe, reason, timeLeft
end
