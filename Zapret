import lupa
from lupa import LuaRuntime
import re
import logging
import asyncio

# Логгер
logger = logging.getLogger("DeobfuscatorEngine")

class DeobfuscatorEngine:
    def __init__(self):
        pass

    def process(self, content, status_callback=None):
        """
        Главный метод обработки.
        """
        def update_status(msg):
            if status_callback: status_callback(msg)
            logger.info(f"[STATUS] {msg}")

        # --- ЭТАП 1: Подготовка (Lua 5.1/5.4) ---
        update_status("🛠 Подготовка 'Черной дыры' (Universal Proxy)...")
        
        try:
            lua = LuaRuntime(unpack_returned_tuples=True)
        except Exception as e:
            return f"-- [FATAL]: Ошибка старта Lua: {e}"

        # --- ЭТАП 2: Полифилл BIT (Обязателен) ---
        # MoonSec и IronBrew жить не могут без битовых операций
        bit_polyfill = """
        local bit = {}
        function bit.tobit(x) x = x % 4294967296; if x >= 2147483648 then x = x - 4294967296 end; return x end
        function bit.bnot(x) return 4294967295 - x % 4294967296 end
        
        local function bit_oper(a, b, oper)
            local r, m, s = 0, 2^31, nil
            repeat
                s,a,b = a+b+m, a%m, b%m
                r,m = r + m*oper%(s-a-b), m/2
            until m < 1
            return r
        end
        
        function bit.band(a, b) return bit_oper(a, b, 4) end
        function bit.bor(a, b)  return bit_oper(a, b, 1) end
        function bit.bxor(a, b) return bit_oper(a, b, 3) end
        function bit.lshift(a, b) return (a * 2^b) % 4294967296 end
        function bit.rshift(a, b) return math.floor(a / 2^b) % 4294967296 end
        
        package.loaded.bit = bit
        _G.bit = bit
        _G.bit32 = bit
        """

        # --- ЭТАП 3: Создание Среды-Ловушки ---
        update_status("🛡️ Активация режима эмуляции эксплойта...")
        
        sandbox_setup = bit_polyfill + """
        _G.CAPTURED = nil
        _G.LOGS = {}
        
        local function log(msg) table.insert(_G.LOGS, msg) end

        -- [[ МАГИЧЕСКИЙ ПРОКСИ ]] --
        -- Этот код создает объекты, которые существуют, даже если их нет.
        local function create_proxy(name)
            local proxy = {}
            local meta = {}
            
            -- Если скрипт обращается к proxy.Field
            meta.__index = function(t, k)
                -- log("[DEBUG] Доступ к полю: " .. name .. "." .. tostring(k))
                return create_proxy(name .. "." .. tostring(k))
            end
            
            -- Если скрипт вызывает proxy(...)
            meta.__call = function(t, ...)
                -- log("[DEBUG] Вызов функции: " .. name .. "()")
                return create_proxy(name .. "()")
            end
            
            -- Математика и строки (чтобы не крашило на if x > 0)
            meta.__tostring = function() return name end
            meta.__len = function() return 0 end
            meta.__add = function() return 0 end
            meta.__sub = function() return 0 end
            meta.__mul = function() return 0 end
            meta.__div = function() return 0 end
            meta.__eq = function() return true end -- Всегда равно true, чтобы проходить проверки
            meta.__lt = function() return false end
            
            -- Метатаблица заблокирована
            meta.__metatable = "LOCKED_BY_DEOBFUSCATOR"
            
            setmetatable(proxy, meta)
            return proxy
        end

        -- [[ ХУК ЗАГРУЗКИ ]] --
        local function hooked_loadstring(chunk, chunkname)
            -- Если передан код (строка) и он длинный - это оно!
            if type(chunk) == "string" and #chunk > 50 then
                _G.CAPTURED = chunk
                log("[!!!] ДЖЕКПОТ! loadstring перехватил " .. #chunk .. " байт кода.")
                return function() end -- Пустышка
            end
            return function() end
        end

        -- [[ СБОРКА ОКРУЖЕНИЯ ]] --
        local env = {
            -- Основные перехватчики
            loadstring = hooked_loadstring,
            load = hooked_loadstring,
            
            -- Реальные библиотеки (обязательно нужны рабочими)
            string = string, math = math, table = table, 
            bit = _G.bit, bit32 = _G.bit,
            pairs = pairs, ipairs = ipairs, next = next,
            type = type, tostring = tostring, tonumber = tonumber,
            pcall = pcall, xpcall = xpcall, select = select,
            error = error, unpack = unpack or table.unpack,
            
            -- Эмуляция окружения эксплойтов (чтобы скрипт чувствовал себя как дома)
            getgenv = function() return _G.sandbox_env end,
            getrenv = function() return _G.sandbox_env end,
            getreg = function() return {} end,
            getgc = function() return {} end,
            checkcaller = function() return true end,
            iscclosure = function() return false end,
            
            -- Имитация Roblox
            game = create_proxy("game"),
            workspace = create_proxy("workspace"),
            script = create_proxy("script"),
            Instance = { new = function(n) return create_proxy("Instance.new("..n..")") end }
        }

        -- ЗАМЫКАНИЕ НА PROXY
        -- Если скрипт просит переменную, которой нет в списке выше (например WhGUwnAR),
        -- мы отдаем ему Proxy, а не nil!
        setmetatable(env, {
            __index = function(t, k)
                -- log("[Global Access] Запрос неизвестной переменной: " .. tostring(k))
                return create_proxy(tostring(k))
            end
        })

        env._G = env
        env.shared = env
        _G.sandbox_env = env
        
        return env
        """

        # --- ЭТАП 4: Исполнение ---
        update_status("🚀 Запуск (режим подавления ошибок)...")
        
        try:
            env = lua.execute(sandbox_setup)
            
            # Оборачиваем исполнение
            runner = lua.eval("""
                function(code, env)
                    local f, err = load(code, "obf_script", "t", env)
                    if not f then return false, "Syntax: " .. tostring(err) end
                    
                    -- Запускаем. Если упадет - не страшно, главное проверить CAPTURED
                    pcall(f)
                    
                    return true, "Executed"
                end
            """)
            
            runner(content, env)
            
            # Проверяем улов
            captured = lua.globals().CAPTURED
            logs = list(lua.globals().LOGS)
            
            if captured:
                update_status("✅ УСПЕХ! Скрытый код найден.")
                return self._clean_code(str(captured))
            else:
                # Если ничего не поймали, возвращаем логи для анализа
                log_str = "\n".join([str(l) for l in logs])
                return f"-- [FAIL] Скрипт отработал, но loadstring не был вызван.\n-- Возможно, он использует 'vm.load' или другую технику.\n\n-- ЛОГИ ПЕСОЧНИЦЫ:\n{log_str}"

        except Exception as e:
            return f"-- [CRASH]: {e}"

    def _clean_code(self, code):
        """Чистка кода после деобфускации"""
        
        # 1. Убираем лишние ; и переформатируем
        code = code.replace(";", "\n")
        
        # 2. Пытаемся восстановить отступы (примитивно)
        lines = code.split('\n')
        indent = 0
        formatted = []
        for line in lines:
            line = line.strip()
            if not line: continue
            
            # Уменьшаем отступ для end, until, else
            if re.match(r"^(end|until|else|elseif)", line):
                indent = max(0, indent - 1)
            
            formatted.append(("\t" * indent) + line)
            
            # Увеличиваем отступ для function, if, do, repeat, {
            # (исключая 'end' в той же строке)
            if re.search(r"\b(function|then|do|repeat)\b", line) and not line.endswith("end"):
                indent += 1
            if line.endswith("{"):
                indent += 1
            if line.endswith("}"): # Закрытие таблицы
                indent = max(0, indent - 1)

        return "-- [[ DEOBFUSCATED RESULT ]]\n\n" + "\n".join(formatted)
