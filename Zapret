    # ... (код выше без изменений)

        # 2. ЗАПУСК
        try:
            # result_code теперь будет типа BYTES
            result_code = await asyncio.wait_for(
                asyncio.to_thread(engine.process, content, sync_status_updater),
                timeout=120.0
            )
        except asyncio.TimeoutError:
            result_code = b"-- [TIMEOUT] Script too long." # Байтовая строка
            await status_msg.edit_text("❌ **Ошибка:** Время вышло.")

        # 3. Сохранение (ИСПРАВЛЕНО: используем "wb")
        await status_msg.edit_text("✅ **Готово!** Загружаю файл...")
        
        # ВАЖНО: "wb" вместо "w", и убираем encoding
        with open(output_path, "wb") as f:
            f.write(result_code) # result_code уже в байтах

        output_file = FSInputFile(output_path, filename=f"Deobf_{file_name}")
        await message.answer_document(output_file, caption="✅ Успешная деобфускация (Binary/Source).")

    # ... (остальной код без изменений)
