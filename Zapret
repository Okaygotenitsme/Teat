local Workspace = game:GetService("Workspace")

local towersFolder = Workspace:FindFirstChild("Towers") or Instance.new("Folder", Workspace)
towersFolder.Name = "Towers"

local function createTowerUI(obj)
	-- Определяем, куда вешать текст (на PrimaryPart или на сам Парт)
	local adornee = (obj:IsA("Model") and obj.PrimaryPart) or (obj:IsA("BasePart") and obj)
	if not adornee then return end
	
	local bgui = Instance.new("BillboardGui", adornee)
	bgui.Name = "HealthGui"
	bgui.Size = UDim2.new(6, 0, 1.5, 0)
	bgui.AlwaysOnTop = true
	bgui.ExtentsOffset = Vector3.new(0, 6, 0) -- Высота текста
	
	local label = Instance.new("TextLabel", bgui)
	label.Size = UDim2.new(1, 0, 1, 0)
	label.BackgroundTransparency = 1
	label.TextColor3 = Color3.new(1, 0.8, 0) -- Золотой цвет
	label.TextStrokeTransparency = 0
	label.TextScaled = true
	label.Text = tostring(obj:GetAttribute("Health") or 0)
	
	-- Обновление текста при уроне
	obj:GetAttributeChangedSignal("Health"):Connect(function()
		label.Text = tostring(math.max(0, math.floor(obj:GetAttribute("Health") or 0)))
	end)
end

local function setupTower(name, health)
	local tower = Workspace:FindFirstChild(name)
	if tower then
		tower.Parent = towersFolder
		tower:SetAttribute("Health", health)
		
		-- Если это модель без PrimaryPart, назначаем любую деталь главной
		if tower:IsA("Model") and not tower.PrimaryPart then
			tower.PrimaryPart = tower:FindFirstChildWhichIsA("BasePart")
		end
		
		createTowerUI(tower)
		print("Башня настроена: " .. name)
	else
		warn("Башня не найдена в Workspace: " .. name)
	end
end

-- Настройка трех башен
setupTower("MainTower", 2000)
setupTower("TowerLeft", 1000)
setupTower("TowerRight", 1000)
